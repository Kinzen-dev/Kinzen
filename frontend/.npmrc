# Force npm to use legacy peer dependency resolution
legacy-peer-deps=true

# Allow npm to install optional dependencies
optional=true

# Set registry to npm (in case of any registry issues)
registry=https://registry.npmjs.org/

# Disable package-lock for better compatibility with Vercel
package-lock=false
